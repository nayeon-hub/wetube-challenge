extends layouts/main 
include mixins/movieBlock


block content 
    h1 searhing by : #{search}
    .movies_search
        each item in movies
            +movieBlock({
                id : item.id,
                title : item.title,
            })